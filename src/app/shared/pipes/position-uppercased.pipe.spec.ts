import { PositionUppercasedPipe } from './position-uppercased.pipe';

describe('PositionUppercasedPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionUppercasedPipe();
    expect(pipe).toBeTruthy();
  });
});
